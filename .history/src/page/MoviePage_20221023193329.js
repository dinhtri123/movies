import React from "react";
import styled from "styled-components";

const MoviePageStyles = styled.div``;
const MoviePage = () => {
  return <MoviePageStyles></MoviePageStyles>;
};

export default MoviePage;
