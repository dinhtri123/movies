import React from "react";
import styled from "styled-components";

const MoviePageStyles = styled.div`
color:white;`;
const MoviePage = () => {
  return <MoviePageStyles>Movie Page</MoviePageStyles>;
};

export default MoviePage;
