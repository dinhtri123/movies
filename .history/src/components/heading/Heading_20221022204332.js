import React from 'react';

const HeadingStyles = import styled from "styled-components";

const Heading = () => {
    return (
        <div>
            
        </div>
    );
};

export default Heading;