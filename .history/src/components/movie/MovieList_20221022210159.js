import React from "react";
import styled from "styled-components";
const MovieListStyles = styled.div``;
const MovieList = () => {
  return <MovieListStyles></MovieListStyles>;
};

export default MovieList;
