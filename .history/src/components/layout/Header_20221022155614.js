import React from 'react';
import styled from "styled-components";
const HeaderStyles = styled.d
const Header = () => {
    return (
        <div>
            
        </div>
    );
};

export default Header;