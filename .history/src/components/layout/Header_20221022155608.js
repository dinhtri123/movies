import React from 'react';
import styled from "styled-components";
const HeaderStyles = 
const Header = () => {
    return (
        <div>
            
        </div>
    );
};

export default Header;