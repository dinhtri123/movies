import React from 'react';
import styled from 'styled-components';

const FooterStyles = styled.
const Footer = () => {
    return (
        <div>
            
        </div>
    );
};

export default Footer;